# Server Environment Configuration Example
# Copy this file to .env and customize values as needed

# Server Configuration
# Port the Express server listens on
PORT=3001

# MongoDB Configuration
# Connection string for MongoDB
MONGO_URL=mongodb://localhost:27017

# Database name for storing Pokémon and battle data
MONGO_DB=pokemon_battles

# Ollama Configuration (Optional - for AI DOOM quotes)
# URL to Ollama server for generating AI victory quotes
# Leave blank to use fallback quotes only
OLLAMA_URL=http://localhost:11434

# Ollama model to use for quote generation
# Recommended: mistral (fast) or llama2 (more creative)
OLLAMA_MODEL=mistral

# Environment mode
# development: verbose logging, no optimizations
# production: minimal logging, optimized performance
NODE_ENV=development
