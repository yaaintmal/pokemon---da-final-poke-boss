# Frontend Environment Configuration Example
# Copy this file to .env.local and customize values as needed

# API Configuration
VITE_API_URL=http://localhost:3001

# Battle System Configuration
# Pokémon stat multiplier range - controls battle balance and variance
# Each Pokémon gets a random multiplier in this range per round

# Minimum multiplier (weaker end)
# Default: 0.6 (allows weak Pokémon to be 60% of normal strength)
# Range: 0.5-0.9 (lower = more chance for underdogs)
VITE_STAT_MULTIPLIER_MIN=0.6

# Maximum multiplier (stronger end)
# Default: 1.87 (allows strong Pokémon to be 187% of normal strength)
# Range: 1.5-2.5 (higher = more extreme variations and upsets)
VITE_STAT_MULTIPLIER_MAX=1.87

# Environment indicator
VITE_ENV=development
